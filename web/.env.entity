# Loaded by `vite build --mode entity` (npm run build:entity-preview).
#
# Turns on the Orbital LLC entity sections so they can be reviewed locally.
# This mode writes to dist-entity/, never to dist/, so it cannot reach a
# deployment by accident. It is a preview only — it does not make the
# company exist.
VITE_ENTITY_PREVIEW=1
